config {
  type: "table"
}

with

source as (

    select * from ${ref('source_source_raw_items')}

),

renamed as (

    select

        ----------  ids
        id as order_item_id,
        order_id,
        sku as product_id

    from source

)

select * from renamed