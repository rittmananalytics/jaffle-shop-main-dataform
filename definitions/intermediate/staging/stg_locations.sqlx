config {
  type: "table"
}

with

source as (
    select * from ${ref('raw_stores')}
),

renamed as (
    select
        id as location_id,
        name as location_name,
        tax_rate,
        DATE_TRUNC('day', opened_at) as opened_date
    from source
)

select * from renamed